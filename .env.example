# Optional. If set, AI summary & open-ended Q&A use an LLM.
# The app works fully offline (rule-based) if none are set.
#
# Provider preference: Gemini is used first, then OpenAI.

# Google Gemini (get a key at https://aistudio.google.com/app/apikey)
GEMINI_API_KEY=
# GEMINI_MODEL=gemini-1.5-flash

# OpenAI (alternative)
OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
